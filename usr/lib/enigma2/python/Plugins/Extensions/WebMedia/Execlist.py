#20200329
def Execlist(session, index):
      if "Iplayer" in index:
             from plugins.Iplayer import Iplayer
             session.open(Iplayer)

      elif "Adultplugs" in index:
             from plugins.Adultplugs import Adultplugs
             session.open(Adultplugs)
      elif "CMT" in index:
             from plugins.CMT import CMT
             session.open(CMT)

      elif "YTsports" in index:
             from plugins.YTsports import YTsports
             session.open(YTsports)

      elif "Daserste" in index:
             from plugins.Daserste import Daserste
             session.open(Daserste)

      elif "RaiTV" in index:
             from plugins.RaiTV import RaiTV
             session.open(RaiTV)

      elif "Dramapk" in index:
             from plugins.Dramapk import Dramapk
             session.open(Dramapk)

      elif "Xrysoi" in index:
             from plugins.Xrysoi import Xrysoi
             session.open(Xrysoi)
             
      elif "Porncom" in index:
             from plugins.Porncom import Porncom
             session.open(Porncom)
      elif "Motherless" in index:
             from plugins.Motherless import Motherless
             session.open(Motherless)

      elif "LuxureTV" in index:
             from plugins.LuxureTV import LuxureTV
             session.open(LuxureTV)

      elif "Porncenter1" in index:
             from plugins.Porncenter1 import Porncenter1
             session.open(Porncenter1)
      elif "Youtube" in index:
             from plugins.Youtube import Youtube
             session.open(Youtube)
      elif "Adulttv" in index:
             from plugins.Adulttv import Adulttv
             session.open(Adulttv)    
      elif "Tivustream18" in index:
             from plugins.Tivustream18 import Tivustream18
             session.open(Tivustream18)   
      elif "TivuStream" in index:
             from plugins.TivuStream import TivuStream
             session.open(TivuStream)
      elif "SportsTV" in index:
             from plugins.SportsTV import SportsTV
             session.open(SportsTV)             
      elif "freearhey" in index:
             from plugins.freearhey import freearhey
             session.open(freearhey)             
      elif "M3uIptv" in index:
             from plugins.M3uIptv import M3uIptv
             session.open(M3uIptv)      
      elif "Worldcam" in index:
             from plugins.Worldcam import Worldcam
             session.open(Worldcam)
      elif "Filmon" in index:
             from plugins.Filmon import Filmon
             session.open(Filmon)
      elif "Exodus" in index:
             from plugins.Exodus import Exodus
             session.open(Exodus)
      elif "Turkvod" in index:
             from plugins.Turkvod import Turkvod
             session.open(Turkvod)
      elif "Showtime" in index:
             from plugins.Showtime import Showtime
             session.open(Showtime)
      elif "Dclip" in index:
             from plugins.Dclip import Dclip
             session.open(Dclip)
      elif "Adulthideout" in index:
             from plugins.Adulthideout import Adulthideout
             session.open(Adulthideout)
      elif "Sportdeutschland" in index:
             from plugins.Sportdeutschland import Sportdeutschland
             session.open(Sportdeutschland)































